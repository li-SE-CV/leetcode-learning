相较于滑动窗口那边的困难题，链表的困难题简单多了

核心思想：统计长度 → 每 k 个节点反转 → 把反转后的部分重新接回链表
对于 nxt = p0.next ##获取原来的头节点的思考：
前面反转后这一组还没有接回原链表
此时链表其实是：
dummy → 1    4 → 5
         ↑
        p0.next
以及
3 → 2 → 1

两部分是断开的

class Solution:
    def reverseKGroup(self, head: Optional[ListNode], k: int) -> Optional[ListNode]:
        # 统计节点个数（只有 >= k 个节点才翻转）
        n = 0
        cur = head
        while cur:
            n += 1
            cur = cur.next

        p0 = dummy = ListNode(next=head)
        pre = None
        cur = head  ##cur → 1，不是哨兵节点dummy

        # k 个一组处理
        while n >= k:
            n -= k
            for _ in range(k):  # 同 92 题翻转链表
                nxt = cur.next
                cur.next = pre  # 每次循环只修改一个 next，方便大家理解
                pre = cur
                cur = nxt

          ##把翻转后的链表接回去
            nxt = p0.next ##获取原来的头节点
            nxt.next = cur ##尾节点连接剩余链表
            p0.next = pre ##前面部分连接反转后的头
            p0 = nxt
        return dummy.next
