使用两个指针分别遍历两个有序数组，模拟归并排序的合并过程，每次取较小元素前进。
只遍历到合并后数组的中位数位置即可，通过 left 和 right 记录当前和前一个元素。
如果总长度为奇数返回 right，否则返回 (left+right)/2。时间复杂度 O(m+n)，空间复杂度 O(1)

代码为：
class Solution:
    def findMedianSortedArrays(self, nums1: List[int], nums2: List[int]) -> float:
        m,n=len(nums1),len(nums2)
        total=m+n
        i,j=0,0
        left,right=0,0
        for _ in range(total//2+1):
            left=right
            if i<m and (j>=n or nums1[i]<nums2[j]):
                right=nums1[i]
                i+=1
            else:
                right=nums2[j]
                j+=1
           
        
        if total%2!=0:
            return right
        else:
            return (left+right)/2
