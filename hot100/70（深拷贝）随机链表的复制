题干太长了（其实就是深拷贝），大致意思是：
深拷贝一个链表，要求新链表中的每个节点都是新创建的
并且这些节点的 random 指针都指向新链表中的相应节点
每个节点有两个指针：
next   → 下一个节点
random → 链表中的任意节点 或 None
例如：
1 → 2 → 3
|   |   |
↓   ↓   ↓
3   1   2   (random 指向)
目标是 创建一个完全新的链表，但结构和 random 指向都一样

逻辑思路：
用哈希表建立
旧节点 → 新节点的映射
分两次遍历：1️⃣ 创建所有新节点 2️⃣ 连接 next 和 random

原链表：
1 → 2 → 3
random:
1 → 3
2 → 1
3 → 2
第一轮：
hash_:
1 → new1
2 → new2
3 → new3

第二轮连接：
new1.next = new2
new1.random = new3

new2.next = new3
new2.random = new1

new3.next = None
new3.random = new2
最终：
new1 → new2 → new3
random 结构完全一致。

代码为：

class Solution:
    def copyRandomList(self, head: 'Optional[Node]') -> 'Optional[Node]':
        if head == None:
            return None
        hash_ = {}
        ##创建所有新节点
        cur = head
        while cur:
            hash_[cur] = Node(cur.val)
            cur = cur.next

        ##连接 next 和 random
        cur = head
        while cur:
            hash_[cur].next = hash_.get(cur.next)
            hash_[cur].random = hash_.get(cur.random)
            cur = cur.next
        
        return hash_[head]
